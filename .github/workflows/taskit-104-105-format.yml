name: TASKIT-104-105 source formatting

on:
  push:
    branches:
      - agent/taskit-104-105-app-shell

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/taskit-104-105-app-shell
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci --no-audit --no-fund
      - run: npx prettier --write src/app/app/layout.tsx src/components/app-shell/app-shell.module.css src/components/app-shell/app-shell.tsx test/app-shell.test.mjs
      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet -- src/app/app/layout.tsx src/components/app-shell/app-shell.module.css src/components/app-shell/app-shell.tsx test/app-shell.test.mjs; then
            echo "No formatting changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/app/layout.tsx src/components/app-shell/app-shell.module.css src/components/app-shell/app-shell.tsx test/app-shell.test.mjs
          git commit -m "style: format responsive app shell"
          git push origin HEAD:agent/taskit-104-105-app-shell
